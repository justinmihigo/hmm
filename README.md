# Honore Manzi portfolio

A responsive portfolio and booking website for Kigali-based dance artist, choreographer, teacher, and actor Honore Manzi Murengezi.

## Development

```bash
npm install
npm run dev
```

Run `npm run build` for a production build and `npm run lint` for static checks.
